import 'package:flutter/material.dart';

class ArchivedTab extends StatelessWidget {
  const ArchivedTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Archived Tab'),
    );
  }
}
